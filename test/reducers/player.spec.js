import { expect } from 'chai';
import player from '../../app/reducers/player';

/** @test {reducers.js} */
describe('reducers', () => {
  /**
   * @test {app/reducers/player.js}
   */
  describe('player', () => {
    it('should handle the initial state', () => {
      expect(true).to.equal(true);
    });
  });
});
